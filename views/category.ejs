<!DOCTYPE html>

<html lang="fr">

	<head>

		<meta charset="utf-8"/>
		<link rel="stylesheet" href="../public/css/global.css" />
		<link rel="stylesheet" href="../public/css/category.css" />
		<title>ArvetoVox - <%= current.name %></title>
		<link rel="shortcut icon" href="../public/images/favicon.ico">

		<link href="https://fonts.googleapis.com/css?family=Roboto+Slab" rel="stylesheet">
	</head>

	<body>
		<header id='header'>
			<div id="banner">
				<a href="/" id="logoLink"><img src="../public/images/header.png" id="logo"></a>
				<h1 id="slogan">Une fenêtre sur le monde de demain...</h1>
			</div>

			<nav id='nav'>
				<ul id="categories">
                    <% for(let i=0; i<categories.length; i++){ %>
                            <% if(categories[i].id == current.id){ %>
                                <a class="activeCategory" href="../category/<%= categories[i].id %>">
                            <% } else{%>
                                <a class="category" href="../category/<%= categories[i].id %>">
                            <% } %>
                                 <li><%= categories[i].name %></li>
                             </a>
                    <% } %>
				</ul>

				<div id="unsigned">
					<a class="authentification" href="/login">
						Se connecter
					</a>
					<a class="authentification" href="/signup">
						S'inscrire
					</a>
				</div>

				<div id="connected">
					<a id="username" class="authentification">
						Bienvenue, user
					</a>
					<a id="disconnect" class="authentification">
						Se déconnecter
					</a>
				</div>

			</nav>
		</header>

		<h2 id="headline">Articles récents</h2>

		<div id="content">

			<ul id="articlesList">

                <% for(let i=0; i<articles.length; i++){ %>

                <a href="../article/<%=  articles[i].id %>"><li class="article">
                    <% if(articles[i].karma > 0) { %>
                    <div class="karmaPlus">+<%= articles[i].karma %></div>

                    <% } else if(articles[i].karma < 0) { %>
                    <div class="karmaMinus"><%= articles[i].karma %></div>

                    <% } else{ %>
                    <div class="karmaNeutral">+<%= articles[i].karma %></div>
                    <% } %>

                    </articles.length;>
					<div class="articleData">
						<h3 class="title">
							<b>  <%= articles[i].title %> </b>
						</h3>
						<div class="author">par <%= articles[i].username %> </div>
					</div>
				</li></a>

                <% } %>

				<% if(articles.length == 0){ %>
					 <b>Wow! Such empty!
					 <br />t(。>_<。)ށ</b>
				 <%}%>

			</ul>

			<div id="sidebar">
				<br />
				<b><%= current.name %></b>
				<br />
				<p id="sidebarText">
                    <%= current.description %>
					<br /><br /><b>Parcourez dès maintenant les <%= articles.length %> articles de cette catégorie,
					ou <a href="/write/<%= current.id %>" id="writeLink"> écrivez le votre</a></b>.

				</p>
			</div>
		</div>


		<footer id="footer">
			<div class="footerItem"><b>Un site de:</b>
				<br>Aurélien DELVAL (lead developper),
				<br>Antoine BLANQUET (chef de projet),
				<br>Sarah DOLIMAN (DRH),
				<br>Jérémy DELAVAQUERIE (trésorier).
			</div>

			<div class="footerItem">
				<b>Qui est derrière tout ça?</b>
				<br />
				Le site a été créé par 4 étudiants en deuxième année de l'ISTY
				pour un projet de fin de semestre.
			</div>

			<div class="footerItem">
				<b>Retrouvez nous sur toute la toile:</b>
				<br />
				<a href="https://github.com/Arveto"><img class="footerIcon" src="../public/images/github.png"></a>
				<a href="https://twitter.com/ArvetoSoftwares"><img class="footerIcon" src="../public/images/twitter.png"></a>

			</div>
		</footer>
	</body>

	<script src="../public/js/home.js"></script>
</html>
